# 成员D：后端业务与文件上传负责人 — 答辩手册

## 一、模块概述

我是本项目的**后端业务与文件上传负责人**，负责申报系统的核心业务接口开发、申报状态流转管理、文件上传功能实现，以及所有项目文档的编写工作。

### 我的职责范围

1. **申报CRUD接口**：创建、查询、更新、删除申报的完整RESTful API
2. **申报状态流转**：提交申报、撤回申报的状态机管理
3. **文件上传**：单文件上传、多文件上传、类型限制、大小限制
4. **项目文档**：项目简介、项目手册、分工说明、日志、答辩手册

### 我负责的文件

| 文件                                                         | 作用                              |
| ------------------------------------------------------------ | --------------------------------- |
| `backend/src/main/java/com/submission/controller/SubmissionController.java` | 申报接口（CRUD + 提交/撤回）      |
| `backend/src/main/java/com/submission/controller/UploadController.java` | 文件上传接口（MultipartFile）     |
| `backend/src/main/java/com/submission/service/SubmissionService.java` | 申报业务逻辑（状态检查）          |
| `backend/src/main/java/com/submission/mapper/SubmissionMapper.java` | 申报数据访问（MyBatis注解）       |
| `backend/src/main/java/com/submission/entity/Submission.java` | 申报实体类（Lombok）              |
| `backend/src/main/java/com/submission/dto/SubmissionDTO.java` | 申报传输对象（验证注解）          |
| `backend/src/main/java/com/submission/dto/Result.java`       | 统一响应结果（泛型）              |
| `backend/src/main/java/com/submission/exception/GlobalExceptionHandler.java` | 全局异常处理（@ControllerAdvice） |
| `docs/项目简介.md`                                           | 项目简介文档                      |
| `docs/项目手册.md`                                           | 项目手册（详细文档）              |
| `docs/环境配置手册.md`                                       | 环境配置手册（换机器运行指南）    |
| `docs/分工与职责说明.md`                                     | 团队分工说明                      |
| `docs/成员A/B/C/D-日志.md`                                   | 4人工作日志                       |
| `docs/成员A/B/C/D-答辩手册.md`                               | 4人答辩手册                       |

---

## 二、技术细节详解

### 1. 申报实体设计（Submission.java）

```java
@Data
public class Submission {
    private Long id;
    private Long userId;
    private String category;
    private String title;
    private String description;
    private String applicantType;
    private String recommendUnit;
    private String sealFile;
    private String workLink;
    private String workFiles;
    private String idCard;
    private String idCardImage;
    private String status;
    private LocalDateTime submitTime;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
}
```

#### 技术要点

- 使用 Lombok `@Data` 注解自动生成 getter/setter
- `id` 使用 Long 类型，数据库自增
- `userId` 关联用户，实现数据隔离
- `status` 字段管理状态：draft（草稿）、submitted（已提交）
- `workFiles` 使用 JSON 字符串存储多个文件名
- 时间字段使用 `LocalDateTime`，自动映射

---

### 2. 申报DTO（SubmissionDTO.java）

```java
@Data
public class SubmissionDTO {
    @NotBlank(message = "申报类别不能为空")
    private String category;
    
    @NotBlank(message = "作品名称不能为空")
    private String title;
    
    @NotBlank(message = "作品描述不能为空")
    private String description;
    
    @NotBlank(message = "申报类型不能为空")
    private String applicantType;
    
    private String recommendUnit;
    private String sealFile;
    private String workLink;
    private String workFiles;
    private String idCard;
    private String idCardImage;
}
```

#### 技术要点

- 使用 `@NotBlank` 注解进行参数验证
- 必填字段：category、title、description、applicantType
- 可选字段：recommendUnit、sealFile、workLink 等
- 与实体类分离，避免暴露敏感字段

---

### 3. 申报Mapper（SubmissionMapper.java）

```java
@Mapper
public interface SubmissionMapper {
    
    @Insert("INSERT INTO t_submission (user_id, category, title, description, applicant_type, " +
            "recommend_unit, seal_file, work_link, work_files, id_card, id_card_image, status, " +
            "submit_time, create_time, update_time) " +
            "VALUES (#{userId}, #{category}, #{title}, #{description}, #{applicantType}, " +
            "#{recommendUnit}, #{sealFile}, #{workLink}, #{workFiles}, #{idCard}, #{idCardImage}, " +
            "#{status}, #{submitTime}, #{createTime}, #{updateTime})")
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(Submission submission);
    
    @Select("SELECT * FROM t_submission WHERE id = #{id}")
    Submission selectById(Long id);
    
    @Select("SELECT * FROM t_submission WHERE user_id = #{userId} ORDER BY create_time DESC")
    List<Submission> selectByUserId(Long userId);
    
    @Update("UPDATE t_submission SET category=#{category}, title=#{title}, description=#{description}, " +
            "applicant_type=#{applicantType}, recommend_unit=#{recommendUnit}, seal_file=#{sealFile}, " +
            "work_link=#{workLink}, work_files=#{workFiles}, id_card=#{idCard}, id_card_image=#{idCardImage}, " +
            "update_time=#{updateTime} WHERE id=#{id}")
    int update(Submission submission);
    
    @Update("UPDATE t_submission SET status=#{status}, submit_time=#{submitTime} WHERE id=#{id}")
    int updateStatus(@Param("id") Long id, @Param("status") String status, @Param("submitTime") LocalDateTime submitTime);
    
    @Delete("DELETE FROM t_submission WHERE id = #{id}")
    int deleteById(Long id);
}
```

#### 技术要点

1. **@Mapper**：MyBatis 自动扫描，Spring Boot 自动注入
2. **注解方式**：使用 `@Insert`、`@Select`、`@Update`、`@Delete` 配置 SQL
3. **参数映射**：使用 `#{}` 占位符，自动映射 Java 对象属性
4. **自增 ID**：使用 `@Options(useGeneratedKeys = true, keyProperty = "id")` 获取数据库自增 ID
5. **驼峰映射**：`map-underscore-to-camel-case: true` 自动映射

---

### 4. 申报Service（SubmissionService.java）

#### 创建申报

```java
@Transactional
public Result<Submission> create(SubmissionDTO dto, Long userId) {
    Submission submission = new Submission();
    submission.setUserId(userId);
    submission.setCategory(dto.getCategory());
    submission.setTitle(dto.getTitle());
    submission.setDescription(dto.getDescription());
    submission.setApplicantType(dto.getApplicantType());
    submission.setRecommendUnit(dto.getRecommendUnit());
    submission.setSealFile(dto.getSealFile());
    submission.setWorkLink(dto.getWorkLink());
    submission.setWorkFiles(dto.getWorkFiles());
    submission.setIdCard(dto.getIdCard());
    submission.setIdCardImage(dto.getIdCardImage());
    submission.setStatus("draft");
    submission.setCreateTime(LocalDateTime.now());
    submission.setUpdateTime(LocalDateTime.now());
    
    submissionMapper.insert(submission);
    
    return Result.success(submission);
}
```

#### 提交申报

```java
@Transactional
public Result<Submission> submit(Long id, Long userId) {
    Submission submission = submissionMapper.selectById(id);
    
    // 检查存在性
    if (submission == null) {
        return Result.error(404, "申报不存在");
    }
    
    // 检查归属
    if (!submission.getUserId().equals(userId)) {
        return Result.error(403, "无权操作此申报");
    }
    
    // 检查状态
    if (!"draft".equals(submission.getStatus())) {
        return Result.error(400, "只能提交草稿状态的申报");
    }
    
    // 检查必填信息
    if (StringUtils.isEmpty(submission.getTitle()) || 
        StringUtils.isEmpty(submission.getDescription()) ||
        StringUtils.isEmpty(submission.getCategory())) {
        return Result.error(400, "请完善申报信息");
    }
    
    // 更新状态
    submissionMapper.updateStatus(id, "submitted", LocalDateTime.now());
    
    // 刷新数据
    submission = submissionMapper.selectById(id);
    return Result.success(submission);
}
```

#### 撤回申报

```java
@Transactional
public Result<Submission> withdraw(Long id, Long userId) {
    Submission submission = submissionMapper.selectById(id);
    
    if (submission == null) {
        return Result.error(404, "申报不存在");
    }
    
    if (!submission.getUserId().equals(userId)) {
        return Result.error(403, "无权操作此申报");
    }
    
    if (!"submitted".equals(submission.getStatus())) {
        return Result.error(400, "只能撤回已提交的申报");
    }
    
    submissionMapper.updateStatus(id, "draft", null);
    
    submission = submissionMapper.selectById(id);
    return Result.success(submission);
}
```

#### 技术要点

1. **@Transactional**：数据库操作原子性，失败自动回滚
2. **状态检查**：每个操作前检查申报状态，防止非法操作
3. **归属检查**：通过 userId 比对，防止越权访问
4. **完整性检查**：提交时检查必填字段
5. **Result 返回**：统一返回格式，包含 code、message、data

---

### 5. 申报Controller（SubmissionController.java）

```java
@RestController
@RequestMapping("/submissions")
@Validated
public class SubmissionController {
    
    @Autowired
    private SubmissionService submissionService;
    
    @PostMapping
    public Result<Submission> create(@Valid @RequestBody SubmissionDTO dto, 
                                     HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.create(dto, userId);
    }
    
    @GetMapping
    public Result<List<Submission>> getList(HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.getList(userId);
    }
    
    @GetMapping("/{id}")
    public Result<Submission> getDetail(@PathVariable Long id, 
                                        HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.getDetail(id, userId);
    }
    
    @PutMapping("/{id}")
    public Result<Submission> update(@PathVariable Long id, 
                                     @Valid @RequestBody SubmissionDTO dto,
                                     HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.update(id, dto, userId);
    }
    
    @DeleteMapping("/{id}")
    public Result<String> delete(@PathVariable Long id, 
                                 HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.delete(id, userId);
    }
    
    @PostMapping("/{id}/submit")
    public Result<Submission> submit(@PathVariable Long id, 
                                     HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.submit(id, userId);
    }
    
    @PostMapping("/{id}/withdraw")
    public Result<Submission> withdraw(@PathVariable Long id, 
                                       HttpServletRequest request) {
        Long userId = (Long) request.getAttribute("userId");
        return submissionService.withdraw(id, userId);
    }
}
```

#### 技术要点

1. **@RestController**：返回 JSON 数据
2. **@RequestMapping("/submissions")**：基础路径
3. **@Validated**：启用参数验证
4. **@Valid @RequestBody**：验证请求体
5. **@PathVariable**：从 URL 路径获取参数
6. **HttpServletRequest.getAttribute("userId")**：获取拦截器设置的 userId

---

### 6. 文件上传（UploadController.java）

```java
@RestController
@RequestMapping("/upload")
public class UploadController {
    
    @Value("${file.upload-path:./uploads/}")
    private String uploadPath;
    
    @PostMapping
    public Result<String> upload(@RequestParam("file") MultipartFile file) {
        if (file.isEmpty()) {
            return Result.error(400, "文件不能为空");
        }
        
        // 生成唯一文件名
        String originalName = file.getOriginalFilename();
        String ext = originalName.substring(originalName.lastIndexOf("."));
        String newName = UUID.randomUUID().toString() + ext;
        
        // 保存文件
        Path path = Paths.get(uploadPath, newName);
        Files.copy(file.getInputStream(), path);
        
        return Result.success(newName);
    }
    
    @PostMapping("/multiple")
    public Result<List<String>> uploadMultiple(@RequestParam("files") MultipartFile[] files) {
        List<String> names = new ArrayList<>();
        
        for (MultipartFile file : files) {
            if (!file.isEmpty()) {
                String originalName = file.getOriginalFilename();
                String ext = originalName.substring(originalName.lastIndexOf("."));
                String newName = UUID.randomUUID().toString() + ext;
                
                Path path = Paths.get(uploadPath, newName);
                Files.copy(file.getInputStream(), path);
                names.add(newName);
            }
        }
        
        return Result.success(names);
    }
}
```

#### 技术要点

1. **MultipartFile**：Spring 的文件上传抽象
2. **UUID.randomUUID()**：生成唯一文件名，防止冲突
3. **Files.copy**：保存文件到磁盘
4. **文件大小限制**：`application.yml` 中配置 `max-file-size: 50MB`
5. **多文件上传**：遍历数组，逐个保存

---

### 7. 全局异常处理（GlobalExceptionHandler.java）

```java
@ControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    @ResponseBody
    public Result<String> handleValidationException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(error -> error.getField() + ": " + error.getDefaultMessage())
            .collect(Collectors.joining(", "));
        return Result.error(400, message);
    }
    
    @ExceptionHandler(Exception.class)
    @ResponseBody
    public Result<String> handleException(Exception e) {
        return Result.error(500, "系统错误：" + e.getMessage());
    }
}
```

#### 技术要点

1. **@ControllerAdvice**：全局异常处理
2. **@ExceptionHandler**：指定处理的异常类型
3. **@ResponseBody**：返回 JSON
4. **参数验证异常**：提取字段错误信息，返回给前端
5. **通用异常**：捕获所有未处理的异常，返回 500

---

### 8. 统一响应结果（Result.java）

```java
@Data
public class Result<T> {
    private Integer code;
    private String message;
    private T data;
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("操作成功");
        result.setData(data);
        return result;
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMessage(message);
        return result;
    }
}
```

#### 技术要点

1. **泛型 `<T>`**：支持任意类型数据
2. **统一格式**：所有接口返回相同结构
3. **静态方法**：`success` 和 `error` 快速创建结果
4. **前端提取**：`res.data.data`（axios 响应体 → Result → 业务数据）

---

## 三、核心代码讲解

### 1. 状态机设计

```
draft（草稿） → submit → submitted（已提交）
     ↑                        ↓
     └────── withdraw ────────┘
```

**讲解**：

- 草稿状态：可编辑、可删除、可提交
- 已提交状态：可查看、可撤回
- 提交时检查必填信息完整性
- 撤回后回到草稿状态，可重新编辑

### 2. 越权访问防护

```java
// 检查归属
if (!submission.getUserId().equals(userId)) {
    return Result.error(403, "无权操作此申报");
}
```

**讲解**：

- 每个操作前检查申报的 userId 是否与当前用户匹配
- 防止用户 A 操作用户 B 的申报
- 返回 403 禁止访问，而不是 404 不存在
- 避免泄露其他用户的数据存在性

### 3. 文件上传保存

```java
String newName = UUID.randomUUID().toString() + ext;
Path path = Paths.get(uploadPath, newName);
Files.copy(file.getInputStream(), path);
```

**讲解**：

- 使用 UUID 生成唯一文件名，防止同名文件覆盖
- 保留原始扩展名，便于前端识别文件类型
- 使用 `Files.copy` 保存文件到指定目录
- 返回文件名，前端拼接完整 URL 访问

---

## 四、接口说明

### 我提供的接口

| 接口                             | 方法   | 作用         | 调用者                   |
| -------------------------------- | ------ | ------------ | ------------------------ |
| `/api/submissions`               | POST   | 创建申报     | 前端 SubmissionCreate.js |
| `/api/submissions`               | GET    | 获取申报列表 | 前端 Home.js             |
| `/api/submissions/{id}`          | GET    | 获取申报详情 | 前端 SubmissionDetail.js |
| `/api/submissions/{id}`          | PUT    | 更新申报     | 前端 SubmissionDetail.js |
| `/api/submissions/{id}`          | DELETE | 删除申报     | 前端 Home.js             |
| `/api/submissions/{id}/submit`   | POST   | 提交申报     | 前端 Home.js             |
| `/api/submissions/{id}/withdraw` | POST   | 撤回申报     | 前端 Home.js             |
| `/api/upload`                    | POST   | 单文件上传   | 前端 SubmissionCreate.js |
| `/api/upload/multiple`           | POST   | 多文件上传   | 前端 SubmissionCreate.js |

### 接口参数和返回

#### 创建申报

```json
// 请求
{
  "category": "science",
  "title": "智能机器人",
  "description": "基于AI的智能机器人项目",
  "applicantType": "individual",
  "workLink": "https://example.com"
}

// 响应
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "category": "science",
    "title": "智能机器人",
    "status": "draft",
    "createTime": "2026-06-11T10:00:00"
  }
}
```

#### 提交申报

```json
// 请求：POST /api/submissions/1/submit
// 无请求体

// 响应
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "status": "submitted",
    "submitTime": "2026-06-11T10:30:00"
  }
}
```

---

## 五、遇到的问题与解决方案

### 问题1：字段名不匹配

**原因**：前端使用 MongoDB 字段名（`_id`、`createdAt`），后端使用 Java 字段名（`id`、`createTime`）
**解决**：前端统一使用 Java 字段名

```javascript
dataIndex: 'createTime', // 之前是 'createdAt'
rowKey: 'id', // 之前是 '_id'
```

### 问题2：响应数据结构不匹配

**原因**：后端返回 `Result<T>`，前端直接访问 `res.data` 获取不到业务数据
**解决**：前端访问 `res.data.data`

```javascript
const res = await submissionAPI.getList();
setSubmissions(res.data.data || []);
```

### 问题3：文件上传路径问题

**原因**：使用相对路径 `/api/upload`，导致跨域或路径错误
**解决**：使用完整 URL `http://localhost:8080/api/upload`

### 问题4：状态流转逻辑复杂

**原因**：申报状态有多种，操作需要检查状态
**解决**：每个操作前检查状态，返回明确的错误信息

```java
if (!"draft".equals(submission.getStatus())) {
    return Result.error(400, "只能提交草稿状态的申报");
}
```

### 问题5：文件上传大小限制

**原因**：默认文件上传大小太小
**解决**：在 `application.yml` 中配置

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
```

### 问题6：越权访问

**原因**：用户可能通过修改 URL 参数访问其他用户的申报
**解决**：每个操作前检查申报的 userId 是否匹配

```java
if (!submission.getUserId().equals(userId)) {
    return Result.error(403, "无权操作此申报");
}
```

---

## 六、答辩要点

### 1. 开场（30秒）

"我是后端业务与文件上传负责人，负责申报系统的核心业务接口开发、申报状态流转管理、文件上传功能实现，以及所有项目文档的编写工作。"

### 2. 技术亮点（1分钟）

- **申报CRUD**：完整的增删改查，使用 MyBatis 注解方式
- **状态机**：draft → submitted → draft，状态流转清晰
- **越权防护**：每次操作检查归属，防止数据泄露
- **文件上传**：单文件/多文件，UUID 命名，大小限制
- **统一响应**：`Result<T>` 泛型，前端统一提取数据
- **全局异常**：`@ControllerAdvice` 统一处理，返回友好错误信息

### 3. 核心代码展示（1分钟）

展示 `SubmissionService.java` 中的状态流转逻辑和 `UploadController.java` 中的文件上传

### 4. 问题解决（1分钟）

讲述响应数据结构不匹配的问题，以及通过 `Result<T>` 统一格式解决的方案

### 5. 总结（30秒）

"我的模块实现了申报系统的核心业务功能，包括申报CRUD、状态流转、文件上传，以及完整的项目文档，确保了申报流程的完整性和系统的可维护性。"

---

**答辩时长**：约 4 分钟

---

**成员D签名**：____________  
**日期**：2026-06-11
